# Background mascot

Save the approved mascot artwork in this directory as:

`payouts_mascot.png`

The Streamlit theme loads it automatically as a softly masked background watermark over the blue-magenta gradient. The application still renders the gradient when the image is absent.
