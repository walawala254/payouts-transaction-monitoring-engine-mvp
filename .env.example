# Public demo is the safe default. Set to false only in a controlled local/private environment.
PAYOUTS_TM_PUBLIC_DEMO=true
PAYOUTS_TM_MAX_UPLOAD_MB=25
PAYOUTS_TM_MAX_UPLOAD_ROWS=100000

# SQLite history is disabled unless public demo mode is also disabled.
PAYOUTS_TM_ENABLE_HISTORY=false
